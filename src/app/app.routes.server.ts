// Server routes configuration for Angular 18
// Route-level render mode configuration was introduced in Angular 19+
// For Angular 18, SSR/prerender is configured via angular.json build options

export const serverRoutes: string[] = [
  // Routes that should be prerendered can be listed here
  // and referenced in angular.json prerender configuration
];
